
&Вместо("ПолучитьСпецификациюСервиса")
Функция Мок_ПолучитьСпецификациюСервиса(ИмяСервиса)
	
	ПараметрыМетода = Мокито.МассивПараметров(ИмяСервиса);
	
	ПрерватьВыполнение = Ложь;
	Результат = МокитоПерехват.АнализВызова(пбп_ПостроительСпецификацииOpenAPI,
		"ПолучитьСпецификациюСервиса",
		ПараметрыМетода,
		ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		Возврат ПродолжитьВызов(ИмяСервиса);
	Иначе
		Возврат Результат;
	КонецЕсли;
	
КонецФункции

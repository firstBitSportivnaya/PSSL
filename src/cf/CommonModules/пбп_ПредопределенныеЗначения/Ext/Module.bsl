// Библиотека проектных подсистем для упрощения разработки архитектуры на 1С: Предприятие 8,
// включая доработку типовых конфигураций.
//
// Copyright First BIT company
//
//   Licensed under the Apache License, Version 2.0 (the "License");
//   you may not use this file except in compliance with the License.
//   You may obtain a copy of the License at
//
//       http://www.apache.org/licenses/LICENSE-2.0
//
//   Unless required by applicable law or agreed to in writing, software
//   distributed under the License is distributed on an "AS IS" BASIS,
//   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//   See the License for the specific language governing permissions and
//   limitations under the License.
//
// URL:    https://github.com/firstBitSportivnaya/PSSL/
//

#Область ПрограммныйИнтерфейс

// Инициализирует предопределенные значения из таблицы.
//
// Параметры:
//  МенеджерОбъекта	 - СправочникМенеджер, ПланВидовХарактеристикМенеджер	 - менеджер объекта.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Таблица с конфликтными элементами
//
Функция ИнициализироватьПредопределенныеЗначения(МенеджерОбъекта) Экспорт
	
	Если Не пбп_ПредопределенныеЗначенияСлужебный.МенеджерВходитВПодсистемуПредопределенных(МенеджерОбъекта) Тогда
		Возврат Новый ТаблицаЗначений;
	КонецЕсли;
	
	ТЗПредопределенныхЗначений = пбп_ПредопределенныеЗначенияСлужебный
		.ТаблицаПредопределенныхЭлементов(МенеджерОбъекта);
	
	ТаблицаКонфликтныхЭлементов = пбп_ПредопределенныеЗначенияСлужебный
		.ОбработатьПредопределенныеЗначения(ТЗПредопределенныхЗначений, МенеджерОбъекта);
	
	Возврат ТаблицаКонфликтныхЭлементов;
	
КонецФункции

// Возвращает ключи хеша на основании менеджера объекта.
//
// Параметры:
//  МенеджерОбъекта	 - СправочникМенеджер, ПланВидовХарактеристикМенеджер - Менеджер обрабатываемого объекта.
// 
// Возвращаемое значение:
//  Структура - Ключи хеша
//    * КлючиХешаЭлемент	 - Структура - Структура ключей хеша для элемента.
//    * КлючиХешаГруппа		 - Структура - Структура ключей хеша для группы.
//
Функция КлючиХеша(МенеджерОбъекта) Экспорт
	
	ОбъектМетаданных = Метаданные.НайтиПоТипу(ТипЗнч(МенеджерОбъекта));
	
	Поля = пбп_ПредопределенныеЗначенияПереопределяемый.ДобавляемыеСтандартныеПоля();
	УдаляемыеСтандартныеПоля = пбп_ПредопределенныеЗначенияПереопределяемый.УдаляемыеСтандартныеПоля();
	
	Поля = пбп_ПереадресацияКлиентСервер.РазностьМассивов(Поля, УдаляемыеСтандартныеПоля);
	
	ДобавляемыеПоля = пбп_ПредопределенныеЗначенияСлужебный.ДобавляемыеПоляОбъектаДляРасчетаХеша(МенеджерОбъекта);
	пбп_ПереадресацияКлиентСервер.ДополнитьМассив(Поля, ДобавляемыеПоля);
	
	Ключи = Новый Структура("КлючиХешаЭлемент, КлючиХешаГруппа", Новый Структура, Новый Структура);
	Для Каждого Поле Из Поля Цикл
		Реквизит = ОбъектМетаданных.Реквизиты.Найти(Поле);
		ЕстьСтандартныйРеквизит = пбп_ПереадресацияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(
			ОбъектМетаданных.СтандартныеРеквизиты,
			Поле);
		
		Если Реквизит = Неопределено И Не ЕстьСтандартныйРеквизит Тогда
			Продолжить;
		КонецЕсли;
		
		Ключи.КлючиХешаЭлемент.Вставить(Поле);
		
		Если ЕстьСтандартныйРеквизит
			Или Реквизит.Использование <> Метаданные.СвойстваОбъектов.ИспользованиеРеквизита.ДляЭлемента Тогда
			Ключи.КлючиХешаГруппа.Вставить(Поле);
		КонецЕсли;
	КонецЦикла;
	
	Возврат Ключи;
	
КонецФункции

// Обновляет хеш предопределенного элемента в регистре, устанавливая при этом ручное изменение.
//
// Параметры:
//  Объект - СправочникОбъект, ПланВидовХарактеристикОбъект	 - предопределенный элемент.
//
Процедура ОбновитьХешПредопределенногоЭлемента(Объект) Экспорт
	
	Флаг = пбп_ПереадресацияКлиентСервер.СвойствоСтруктуры(Объект.ДополнительныеСвойства, "РучноеИзменение", Истина);
	
	РегистрыСведений.пбп_СостоянияПредопределенныхЭлементов.ОбновитьХешЭлемента(Объект.Ссылка, Объект.ЭтоГруппа, Флаг);
	
КонецПроцедуры

// Создает запись регистра с заданными параметрами.
//
// Параметры:
//  Объект		 - СправочникСсылка, ПланВидовХарактеристикСсылка	 - Ссылка на предопределенный элемент.
//  ЭтоГруппа	 - Булево - - Признак того что элемент является группой.
//  КлючиХеша	 - Структура, Неопределено - Если ключи хеша не переданы, то они будут сформированы заново.
//
Процедура СоздатьЗаписьСостоянияПредопределенногоЭлемента(Объект, ЭтоГруппа, КлючиХеша = Неопределено) Экспорт
	РегистрыСведений.пбп_СостоянияПредопределенныхЭлементов.СоздатьЗаписьРегистра(Объект, ЭтоГруппа, КлючиХеша);
КонецПроцедуры

// Обновляет флаг ручного изменения элемента
//
// Параметры:
//  Элемент  - СправочникСсылка, ПланВидовХарактеристикСсылка - Ссылка на предопределенный элемент
//  Флаг     - Булево - Устанавливается если были изменены ключевые поля пользователем.
//
Процедура УстановитьФлагРучноеИзменение(Элемент, Флаг = Ложь) Экспорт
	РегистрыСведений.пбп_СостоянияПредопределенныхЭлементов.ОбновитьФлагРучногоИзменения(Элемент, Флаг);
КонецПроцедуры

// Заполнение предопределенных элементов, с возможностью серверного оповещения
// о существующих конфликтах при создании элементов.
//
// Параметры:
//  ОтправитьСерверноеОповещение - Булево - В случае необходимости оповещения пользователей, установить флаг в Истину.
//
Процедура ЗаполнениеПредопределенныхЭлементов(ОтправитьСерверноеОповещение = Ложь) Экспорт
	
	Адресаты = пбп_ОбщегоНазначенияСервер.ПолучитьАдресатовСПолнымиПравами();
	
	Типы = Метаданные.ОпределяемыеТипы.пбп_ПредопределенныеВсеСсылкиПереопределяемый.Тип.Типы();
	
	Для Каждого Тип Из Типы Цикл
		ОбъектМетаданных = Метаданные.НайтиПоТипу(Тип);
		ПолноеИмя = ОбъектМетаданных.ПолноеИмя();
		
		Менеджер = пбп_Переадресация.МенеджерОбъектаПоПолномуИмени(ПолноеИмя);
		
		Если Не ОтправитьСерверноеОповещение Тогда
			ИнициализироватьПредопределенныеЗначения(Менеджер);
			Продолжить;
		КонецЕсли;
		
		ПредставлениеОбъекта = ?(ПустаяСтрока(ОбъектМетаданных.ПредставлениеОбъекта),
			ОбъектМетаданных.Синоним,
			ОбъектМетаданных.ПредставлениеОбъекта);
		
		ТаблицаКонфликтныхЭлементов = ИнициализироватьПредопределенныеЗначения(Менеджер);
		
		Если ЗначениеЗаполнено(ТаблицаКонфликтныхЭлементов) Тогда
			Результат = Новый Структура;
			Результат.Вставить("ИмяОбъектаМетаданных", ПолноеИмя);
			Результат.Вставить("ПредставлениеОбъекта", ПредставлениеОбъекта);
			
			пбп_Переадресация.ОтправитьСерверноеОповещение(
				пбп_ПредопределенныеЗначенияСлужебный.ИмяСерверногоОповещения(),
				Результат,
				Адресаты);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

// Выполняет действия при записи предопределенного элемента.
//
// Параметры:
//  Объект - СправочникОбъект, ПланВидовХарактеристикОбъект - предопределенный элемент
//                             из подсистемы предопределенных элементов
//
Процедура ОбработатьСуществующийПредопределенныйЭлемент(Объект) Экспорт
	
	Если пбп_ПереадресацияКлиентСервер.СвойствоСтруктуры(Объект.ДополнительныеСвойства, "ЭтоНовый", Ложь) Тогда
		Возврат;
	КонецЕсли;
	
	ИмяРеквизита = пбп_ПредопределенныеЗначенияСлужебный.ИмяПоляПредопределенности();
	Если пбп_ПереадресацияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Объект, ИмяРеквизита)
		И Не Объект.ЭтоПредопределенный Тогда
		Возврат;
	КонецЕсли;
	
	ОбновитьХешПредопределенногоЭлемента(Объект);
	
КонецПроцедуры

#Область Обработчики

// Вызывается из одноименного обработчика ПередЗаписью.
//
// Параметры:
//  Объект	 - СправочникОбъект, ПланВидовХарактеристикОбъект - редопределенный элемент
//                             из подсистемы предопределенных элементов.
//  Отказ	 - Булево - Признак отказа от записи.
//
Процедура ПередЗаписью(Объект, Отказ) Экспорт
	
	Объект.ДополнительныеСвойства.Вставить("ЭтоНовый", Объект.ЭтоНовый());
	
	УстановитьФлагПредопределенногоЭлемента(Объект);
	
КонецПроцедуры

// Вызывается из одноименного обработчика ПриЗаписи.
//
// Параметры:
//  Объект	 - СправочникОбъект, ПланВидовХарактеристикОбъект - редопределенный элемент
//                             из подсистемы предопределенных элементов.
//  Отказ	 - Булево - Признак отказа от записи.ы
//
Процедура ПриЗаписи(Объект, Отказ) Экспорт
	
	ОбработатьСуществующийПредопределенныйЭлемент(Объект);
	
КонецПроцедуры

// Выводит подключенные команды в форме.
// Для вызова из обработчика формы ПриСозданииНаСервере.
//
// Если в форме несколько списков, то в обработчике формы ПриСозданииНаСервере
// следует разместить несколько вызовов этой процедуры с указанием параметра ПараметрыРазмещения.
// Параметр ПараметрыРазмещения также применяется, когда типы источников зависят от параметров открытия формы.
//
// Параметры:
//   Форма - ФормаКлиентскогоПриложения - форма, в которой необходимо разместить команды.
//   ПараметрыРазмещения - см. пбп_ПредопределенныеЗначения.ПараметрыРазмещения
//                       - Неопределено
//
Процедура ПриСозданииНаСервере(Форма, Знач ПараметрыРазмещения = Неопределено) Экспорт
	
	ПереданныеПараметрыРазмещения = ПараметрыРазмещения;
	
	ПараметрыРазмещения = ПараметрыРазмещения();
	Если ПереданныеПараметрыРазмещения <> Неопределено Тогда
		ЗаполнитьЗначенияСвойств(ПараметрыРазмещения, ПереданныеПараметрыРазмещения);
	КонецЕсли;
	
	СоздатьКомандуЗаполнения(Форма);
	
КонецПроцедуры

#КонецОбласти

// Конструктор одноименного параметра процедуры пбп_ПредопределенныеЗначения.ПриСозданииНаСервере.
//
// Возвращаемое значение:
//   Структура - параметры размещения подключаемых команд:
//       * Источники - ОписаниеТипов
//                   - Массив из ОбъектМетаданных - источники команд.
//           Используется для второстепенных списков, а также в формах объектов, не являющихся поставщиками
//           команд (обработки, общие формы).
//       * КоманднаяПанель - ГруппаФормы - командная панель или группа команд, в которой выводятся подменю.
//           Используется как родитель для создания подменю в случае их отсутствия.
//           Если не указан то в первую очередь ищется группа "ПодключаемыеКоманды".
//       * ПрефиксГрупп - Строка - добавка к именам подменю и имени командной панели.
//           Используется при необходимости префиксации групп с командами (в частности, когда в форме несколько таблиц).
//           В качестве префикса рекомендуется использовать имя таблицы формы, для которой выводятся команды.
//           Например, если ПрефиксГрупп = "СкладскиеДокументы" (имя второстепенной таблицы формы),
//           то используются подменю с именами "СкладскиеДокументыПодменюПечать", "СкладскиеДокументыПодменюОтчеты" и т.д.
//       * ВладелецКоманд - ДанныеФормыСтруктура, ТаблицаФормы - объект или элемент формы, для которого выводятся команды.
//
Функция ПараметрыРазмещения() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("Источники");
	Результат.Вставить("КоманднаяПанель");
	Результат.Вставить("ПрефиксГрупп", "");
	Результат.Вставить("ВладелецКоманд");
	
	Возврат Результат;
	
КонецФункции

#Область ПолучениеПредопределенных

// Возвращает ссылку на предопределенный элемент
//
// Параметры:
//  ИсточникМетаданных		 - СправочникМенеджер, ПланВидовХарактеристикМененджер, ПланСчетовМенеджер, ПланВидовРасчетовМенеджер, Строка - источник,
//                             по которому определяется объект метаданных с предопределёнными элементами.
//                             Для Строки ожидается полное имя объекта в синтаксисе 1С (например, ,
//                             "Справочник.пбп_НастройкиИнтеграции"). Для менеджера используется его тип (например,
//                             СправочникМенеджер.Номенклатура, ПланСчетовМенеджер.Хозрасчетный).
//  Идентификатор			 - Строка - идентификатор предопределенного элемента.
//  ВыбрасыватьИсключение	 - Булево - выбрасывать исключение, если есть ошибки.
//  ЗначениеПоУмолчанию		 - СправочникСсылка, ПланВидовХарактеристикСсылка, ПланСчетовСсылка, ПланВидовСсылка, Неопределено - значение,
//                             которое возвращается, если предопределенного не существует.
//                             Если не указано, возвращается значение Неопределено.
//  ИспользоватьКэш			 - Булево - признак получения данных из кэша.
// 
// Возвращаемое значение:
//  СправочникСсылка, ПланВидовХарактеристикСсылка, ПланСчетовСсылка, ПланВидовСсылка, Неопределено - Ссылка на предопределенный элемент.
//
Функция ПредопределенныйЭлемент(ИсточникМетаданных,
	Знач Идентификатор,
	ВыбрасыватьИсключение = Ложь,
	ЗначениеПоУмолчанию = Неопределено,
	ИспользоватьКэш = Ложь) Экспорт
	
	Значение = ЗначениеПоУмолчанию;
	
	ИмяОбъектаМетаданных = ИмяОбъектаМетаданныхПоИсточнику(ИсточникМетаданных, ВыбрасыватьИсключение);
	Если ИмяОбъектаМетаданных = Неопределено Тогда
		Возврат Значение;
	КонецЕсли;
	
	Если ИспользоватьКэш Тогда
		Возврат пбп_ПредопределенныеЗначенияПовтИсп.ПредопределенныйЭлемент(
			ИмяОбъектаМетаданных,
			Идентификатор,
			ВыбрасыватьИсключение,
			ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Возврат ПолучитьПредопределенныеПоИдентификаторам(ИмяОбъектаМетаданных,
		Идентификатор,
		Значение,
		ВыбрасыватьИсключение);
	
КонецФункции

// Возвращает ссылки на предопределенные элементы, либо массивом, либо в разрезе ключе соответствием
//
// Параметры:
//  ИсточникМетаданных		 - СправочникМенеджер, ПланВидовХарактеристикМененджер, ПланСчетовМенеджер, ПланВидовРасчетовМенеджер, Строка - источник,
//                             по которому определяется объект метаданных с предопределёнными элементами.
//                             Для Строки ожидается полное имя объекта в синтаксисе 1С (например, ,
//                             "Справочник.пбп_НастройкиИнтеграции"). Для менеджера используется его тип (например,
//                             СправочникМенеджер.Номенклатура, ПланСчетовМенеджер.Хозрасчетный).
//  Идентификаторы - Строка - идентификаторы, перечисленные через запятую, в формате
//                       требований к свойствам структуры.
//                       Например, "RabbitMQ, Kafka, 1СШина".
//                 - Массив из Строка
//                 - ФиксированныйМассив из Строка - идентификаторы в формате требований к свойствам структуры.
//  ВыбрасыватьИсключение	 - Булево - выбрасывать исключение, если есть ошибки.
//  ВРазрезеКлючей			 - Булево - признак получения данных в виде соответствия, где ключ - Идентификатор.
// 
// Возвращаемое значение:
//  Массив - Содержит ссылки на предопределенные элементы в порядке, в котором были переданы идентификаторы.
//  Соответствие - содержит идентификаторы (ключи) и ссылки затребованных предопределенных элементов.
//                 Если в параметр Идентификаторы передана пустая строка, то возвращается пустое соответствие.
//
Функция ПредопределенныеЭлементы(ИсточникМетаданных,
	Знач Идентификаторы,
	ВыбрасыватьИсключение = Ложь,
	ИспользоватьКэш = Ложь) Экспорт
	
	Значения = Новый Массив;
	
	ИмяОбъектаМетаданных = ИмяОбъектаМетаданныхПоИсточнику(ИсточникМетаданных, ВыбрасыватьИсключение);
	Если ИмяОбъектаМетаданных = Неопределено Тогда
		Возврат Значения;
	КонецЕсли;
	
	Если ИспользоватьКэш Тогда
		Возврат пбп_ПредопределенныеЗначенияПовтИсп.ПредопределенныеЭлементы(
			ИмяОбъектаМетаданных,
			Идентификаторы,
			ВыбрасыватьИсключение);
	КонецЕсли;
	
	Возврат ПолучитьПредопределенныеПоИдентификаторам(
		ИмяОбъектаМетаданных, Идентификаторы,
		Значения,
		Истина,
		ВыбрасыватьИсключение);
	
КонецФункции

// Возвращает значение предопределенного элемента
//
// Параметры:
//  ИсточникМетаданных		 - СправочникМенеджер, ПланВидовХарактеристикМененджер, ПланСчетовМенеджер, ПланВидовРасчетовМенеджер, Строка - источник,
//                             по которому определяется объект метаданных с предопределёнными элементами.
//                             Для Строки ожидается полное имя объекта в синтаксисе 1С (например, ,
//                             "Справочник.пбп_НастройкиИнтеграции"). Для менеджера используется его тип (например,
//                             СправочникМенеджер.Номенклатура, ПланСчетовМенеджер.Хозрасчетный).
//  Ссылка					 - ЛюбаяСсылка - ссылка на предопределенный элемент.
//  ВыбрасыватьИсключение	 - Булево - выбрасывать исключение, если есть ошибки.
//  ЗначениеПоУмолчанию		 - СправочникСсылка, ПланВидовХарактеристикСсылка, ПланСчетовСсылка, ПланВидовСсылка, Неопределено - значение,
//                             которое возвращается, если предопределенного не существует.
//                             Если не указано, возвращается значение Неопределено.
//  ИспользоватьКэш			 - Булево - признак получения данных из кэша.
// 
// Возвращаемое значение:
//  Произвольный - значение предопределенного элемента.
//
Функция ЗначениеПредопределенногоЭлемента(ИсточникМетаданных,
	Ссылка,
	ВыбрасыватьИсключение = Ложь,
	ЗначениеПоУмолчанию = Неопределено,
	ИспользоватьКэш = Ложь) Экспорт
	
	ИмяОбъектаМетаданных = ИмяОбъектаМетаданныхПоИсточнику(ИсточникМетаданных, ВыбрасыватьИсключение);
	Если ИмяОбъектаМетаданных = Неопределено Тогда
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	
	Если ИспользоватьКэш Тогда
		Возврат пбп_ПредопределенныеЗначенияПовтИсп.ЗначениеПредопределенногоЭлемента(
			ИмяОбъектаМетаданных,
			Ссылка,
			ВыбрасыватьИсключение,
			ЗначениеПоУмолчанию);
	КонецЕсли;
	
	Менеджер = пбп_Переадресация.МенеджерОбъектаПоПолномуИмени(ИмяОбъектаМетаданных);
	
	Возврат Менеджер.ПолучитьЗначение(Ссылка, ВыбрасыватьИсключение, ЗначениеПоУмолчанию);
	
КонецФункции

// Возвращает значения предопределенных элементов
//
// Параметры:
//  ИсточникМетаданных		 - СправочникМенеджер, ПланВидовХарактеристикМененджер, ПланСчетовМенеджер, ПланВидовРасчетовМенеджер, Строка - источник,
//                             по которому определяется объект метаданных с предопределёнными элементами.
//                             Для Строки ожидается полное имя объекта в синтаксисе 1С (например, ,
//                             "Справочник.пбп_НастройкиИнтеграции"). Для менеджера используется его тип (например,
//                             СправочникМенеджер.Номенклатура, ПланСчетовМенеджер.Хозрасчетный).
//  Ссылки					 - Массив из ЛюбаяСсылка - ссылки на предопределенные элементы.
//  ВыбрасыватьИсключение	 - Булево - выбрасывать исключение, если есть ошибки.
//  ВРазрезеКлючей			 - Булево - признак получения данных в виде соответствия, где ключ - Ссылка.
// 
// Возвращаемое значение:
//  Массив 			 - содержит значения предопределенных элементов в порядке, в котором были переданы ссылки.
//  Соответствие	 - содержит ссылки (ключи) и значения затребованных предопределенных элементов.
//                     Если в параметр Ссылки передан пустой массив, то возвращается пустое соответствие.
//
Функция ЗначенияПредопределенныхЭлементов(ИсточникМетаданных,
	Ссылки,
	ВыбрасыватьИсключение = Ложь,
	ВРазрезеКлючей = Ложь,
	ИспользоватьКэш = Ложь) Экспорт
	
	ИмяОбъектаМетаданных = ИмяОбъектаМетаданныхПоИсточнику(ИсточникМетаданных, ВыбрасыватьИсключение);
	Если ИмяОбъектаМетаданных = Неопределено Тогда
		Если ВРазрезеКлючей Тогда
			Возврат Новый Соответствие;
		Иначе
			Возврат Новый Массив;
		КонецЕсли;
	КонецЕсли;
	
	Если ИспользоватьКэш Тогда
		Возврат пбп_ПредопределенныеЗначенияПовтИсп.ЗначенияПредопределенныхЭлементов(
			ИмяОбъектаМетаданных,
			Ссылки,
			ВыбрасыватьИсключение,
			ВРазрезеКлючей);
	КонецЕсли;
	
	Менеджер = пбп_Переадресация.МенеджерОбъектаПоПолномуИмени(ИмяОбъектаМетаданных);
	
	Возврат Менеджер.ПолучитьЗначения(Ссылки, ВыбрасыватьИсключение, ВРазрезеКлючей);
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

Процедура ДополнитьЗначенияПредопределенных(Приемник, Значение, КлючЗначения, МножественныйВыбор = Ложь) Экспорт

	Если МножественныйВыбор Тогда
		Если ТипЗнч(Приемник) = Тип("Соответствие") Тогда
			Приемник.Вставить(КлючЗначения, Значение);
		Иначе
			Если ТипЗнч(Значение) = Тип("Массив") Тогда
				пбп_ПереадресацияКлиентСервер.ДополнитьМассив(Приемник, Значение);
			Иначе
				Приемник.Добавить(Значение);
			КонецЕсли;
		КонецЕсли;
	Иначе
		Приемник = Значение;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура УстановитьФлагПредопределенногоЭлемента(Объект)
	
	ИмяРеквизита = пбп_ПредопределенныеЗначенияСлужебный.ИмяПоляПредопределенности();
	Если Не пбп_ПереадресацияКлиентСервер.ЕстьРеквизитИлиСвойствоОбъекта(Объект, ИмяРеквизита) Тогда
		Возврат;
	КонецЕсли;
	
	Объект.ЭтоПредопределенный = ЗначениеЗаполнено(Объект.ИдентификаторНастройки);
	
КонецПроцедуры

Функция КоманднаяПанельФормы(Форма, ПрефиксГрупп, ЭтоФормаОбъекта)
	
	Элементы = Форма.Элементы;
	
	Результат = Элементы.Найти(ПрефиксГрупп + "ПодключаемыеКоманды");
	Если Результат = Неопределено Тогда
		Результат = Элементы.Найти(ПрефиксГрупп + "КоманднаяПанель");
		
		Если Результат = Неопределено Тогда
			Результат = Элементы.Найти(ПрефиксГрупп + "ОсновнаяКоманднаяПанель");
			
			Если Результат = Неопределено И ЗначениеЗаполнено(ПрефиксГрупп) Тогда
				ТаблицаФормы = Элементы.Найти(ПрефиксГрупп);
				Если ТипЗнч(ТаблицаФормы) = Тип("ТаблицаФормы") Тогда
					Результат = ТаблицаФормы.КоманднаяПанель;
				КонецЕсли;
			КонецЕсли;
			
			Если Не ЭтоФормаОбъекта
				И Результат = Неопределено
				И Не ЗначениеЗаполнено(ПрефиксГрупп) Тогда
				ТаблицаФормы = Элементы.Найти("Список");
				Если ТипЗнч(ТаблицаФормы) = Тип("ТаблицаФормы")
					И ТаблицаФормы.ПоложениеКоманднойПанели <> ПоложениеКоманднойПанелиЭлементаФормы.Нет Тогда
					Результат = ТаблицаФормы.КоманднаяПанель;
				КонецЕсли;
			КонецЕсли;
			
			Если Результат = Неопределено Тогда
				Результат = Форма.КоманднаяПанель;
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Процедура СоздатьКомандуЗаполнения(Форма)
	
	Идентификатор = "_" + СтрЗаменить(Новый УникальныйИдентификатор, "-", "");
	ИмяКоманды = пбп_ПредопределенныеЗначенияКлиентСервер.ИмяКомандыЗаполненияПредопределенных() + Идентификатор;
	ИмяДействия = "Подключаемый_ПредопределенныеВыполнитьКоманду";
	Представление = НСтр("ru = 'Заполнить предопределенные'; en = 'Fill in the predefined'");
	
	пбп_РаботаСФормами.СоздатьКоманду(Форма, ИмяКоманды, Представление, ИмяДействия);
	
	КомандаФормы = Форма.Команды.Найти(ИмяКоманды);
	КомандаФормы.Отображение = ОтображениеКнопки.КартинкаИТекст;
	КомандаФормы.Картинка = БиблиотекаКартинок.ВосстановитьЗначения;
	
	КоманднаяПанель = КоманднаяПанельФормы(Форма, "", Ложь);
	
	ВидКнопки = 3;
	пбп_РаботаСФормами.СоздатьКнопка(Форма, ИмяКоманды, КоманднаяПанель, , ИмяКоманды, ВидКнопки);

КонецПроцедуры

#Область ПолучениеПредопределенных

Функция ПолучитьПредопределенныеПоИдентификаторам(ИмяОбъектаМетаданных,
	Знач Идентификаторы,
	Знач Результат,
	МножественныйВыбор = Ложь,
	ВыбрасыватьИсключение = Ложь) Экспорт
	
	Идентификаторы = ПреобразоватьИдентификаторы(Идентификаторы);
	Если Не ЗначениеЗаполнено(Идентификаторы) Тогда
		Если ВыбрасыватьИсключение Тогда
			ВызватьИсключение (пбп_ПереадресацияКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("ru = 'Неверный тип второго параметра ''%1'', ожидалось <Строка> или <Массив>'"),
				"Идентификаторы"),
				КатегорияОшибки.ОшибкаКонфигурации);
		КонецЕсли;
		
		Возврат Результат;
	КонецЕсли;
	
	// BSLLS:QueryParseError-off
	ТекстЗапроса = 
		"ВЫБРАТЬ
		|	ПредопределенныеЗначения.Ссылка КАК Значение,
		|	ПредопределенныеЗначения.ИдентификаторНастройки КАК Идентификатор
		|ИЗ
		|	%1 КАК ПредопределенныеЗначения
		|ГДЕ
		|	ПредопределенныеЗначения.ИдентификаторНастройки В (&Идентификаторы)";
	// BSLLS:QueryParseError-on

	ТекстЗапроса = СтрШаблон(ТекстЗапроса, ИмяОбъектаМетаданных);
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("Идентификаторы", Идентификаторы);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Если Не МножественныйВыбор И РезультатЗапроса.Пустой() Тогда
		Если ВыбрасыватьИсключение Тогда
			ТекстОшибки = СтрШаблон(
				НСтр("ru = 'Предопределенный элемент не найден: идентификатор %1.';
				|en = 'Predefined element not found: identifier %1.'"),
				Идентификаторы[0]
			);
			ВызватьИсключение (ТекстОшибки, КатегорияОшибки.ОшибкаКонфигурации);
		КонецЕсли;
		
		Возврат Результат;
	КонецЕсли;
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Для Индекс = 0 По Идентификаторы.ВГраница() Цикл
		Значение = Неопределено;
		Если Выборка.НайтиСледующий(Идентификаторы[Индекс]) Тогда
			Значение = Выборка.Значение;
		КонецЕсли;
		Выборка.Сбросить();
		ДополнитьЗначенияПредопределенных(Результат, Значение, Идентификаторы[Индекс], МножественныйВыбор);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ИмяОбъектаМетаданныхПоИсточнику(ИсточникМетаданных, ВыбрасыватьИсключение)
	
	Результат = ПолучитьИмяОбъектаМетаданных(ИсточникМетаданных);
	Если ЗначениеЗаполнено(Результат.Ошибка) Тогда
		Если ВыбрасыватьИсключение Тогда
			ВызватьИсключение (Результат.Ошибка, КатегорияОшибки.ОшибкаКонфигурации);
		КонецЕсли;
		
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Результат.Значение;
	
КонецФункции

Функция ПолучитьИмяОбъектаМетаданных(Источник)
	
	Результат = Новый Структура("Значение,Ошибка", "", "");
	
	Если ТипЗнч(Источник) = Тип("Строка") Тогда
		ПолноеИмя = СокрЛП(Источник);
		Если ПустаяСтрока(ПолноеИмя) Тогда
			Результат.Ошибка = НСтр("ru = 'Не задано имя метаданных'; en = 'Metadata name is empty'");
			Возврат Результат;
		КонецЕсли;
		
		Мета = Метаданные.НайтиПоПолномуИмени(Источник);
		
		Если Мета = Неопределено Тогда
			Результат.Ошибка = СтрШаблон(
				НСтр("ru = 'Объект метаданных не найден: %1'; en = 'Metadata not found: %1'"),
				ПолноеИмя
			);
			Возврат Результат;
		КонецЕсли;
		
		Результат.Значение = Мета.ПолноеИмя();
		Возврат Результат;
	КонецЕсли;
	
	Мета = Метаданные.НайтиПоТипу(ТипЗнч(Источник));
	
	Если Мета = Неопределено Тогда
		Результат.Ошибка = СтрШаблон(
			НСтр("ru = 'Не удалось определить объект метаданных по типу: %1'; en = 'Cannot resolve metadata by type: %1'"),
			ТипЗнч(Источник)
		);
		Возврат Результат;
	КонецЕсли;
		
	Результат.Значение = Мета.ПолноеИмя();
		
	Возврат Результат;
	
КонецФункции

Функция ПреобразоватьИдентификаторы(Знач Идентификаторы)
	
	// Разбор идентификаторов, если второй параметр Строка.
	Если ТипЗнч(Идентификаторы) = Тип("Строка") Тогда
		Если ПустаяСтрока(Идентификаторы) Тогда
			Возврат Неопределено;
		КонецЕсли;
		
		Идентификаторы = СтрРазделить(Идентификаторы, ",", Ложь);
		Для Индекс = 0 По Идентификаторы.ВГраница() Цикл
			Идентификаторы[Индекс] = СокрЛП(Идентификаторы[Индекс]);
		КонецЦикла;
	КонецЕсли;
	
	Если ТипЗнч(Идентификаторы) <> Тип("Массив")
		И ТипЗнч(Идентификаторы) <> Тип("ФиксированныйМассив") Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Идентификаторы;
	
КонецФункции

#КонецОбласти

#КонецОбласти

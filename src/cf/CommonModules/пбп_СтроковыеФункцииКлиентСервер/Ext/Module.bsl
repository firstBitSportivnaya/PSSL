// Библиотека проектных подсистем для упрощения разработки архитектуры на 1С: Предприятие 8,
// включая доработку типовых конфигураций.
//
// Copyright First BIT company
//
//   Licensed under the Apache License, Version 2.0 (the "License");
//   you may not use this file except in compliance with the License.
//   You may obtain a copy of the License at
//
//       http://www.apache.org/licenses/LICENSE-2.0
//
//   Unless required by applicable law or agreed to in writing, software
//   distributed under the License is distributed on an "AS IS" BASIS,
//   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//   See the License for the specific language governing permissions and
//   limitations under the License.
//
// URL:    https://github.com/firstBitSportivnaya/PSSL/
//

////////////////////////////////////////////////////////////////////////////////
// Работа со строками (клиент, сервер): Модуль содержит методы работы со строками.

#Область ПрограммныйИнтерфейс

// Разбирает строку с разделителями и упаковывает значения в массив.
// 
// Параметры:
//  ИсходнаяСтрока  - Строка
//  Разделитель     - Строка
//  ПоНарастающей   - Булево (По умолчанию = Ложь) .
//
// ВозвращаемоеЗначение:
//  Массив
//
Функция РазобратьСтрокуСРазделителями(ИсходнаяСтрока, Разделитель, ПоНарастающей = Ложь) Экспорт
	
	Результат = Новый Массив;
	
	Список = пбп_ОбщегоНазначенияКлиентСервер.РазобратьСтрокуСРазделителями(ИсходнаяСтрока, Разделитель);
	Если ПоНарастающей Тогда
		НарастающаяСтрока = "";
		Для Каждого СтрокаСписка Из Список Цикл
			
			НарастающаяСтрока = ?(ПустаяСтрока(НарастающаяСтрока),
				СтрокаСписка, СтрШаблон("%1.%2", НарастающаяСтрока, СтрокаСписка));
			Результат.Добавить(НарастающаяСтрока);
		КонецЦикла; 
	Иначе
		Результат = Список;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает текст с добавлением/удаление разделителя "|".
// 
// Параметры:
//  Текст    - Строка - Обрабатываемый текст.
//  Добавить - Булево - Признак добавления/удаления вертикальной черты.
// 
// Возвращаемое значение:
//  Строка - Результат обработки строки.
//
Функция УбратьДобавитьВертикальнуюЧерту(Текст, Добавить = Ложь) Экспорт
	
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст(Текст);
	КоличествоСтрок = ТекстовыйДокумент.КоличествоСтрок();
	
	Если Добавить Тогда
		
		Для Сч = 1 По КоличествоСтрок Цикл
		
			ТекущаяСтрока = ТекстовыйДокумент.ПолучитьСтроку(Сч);
			
			Если Сч = 1 Тогда
				ТекущаяСтрока = """" + ТекущаяСтрока;
			Иначе
				
				ТекущаяСтрока = СтрЗаменить(ТекущаяСтрока, """", """""");
				ТекущаяСтрока = СтрШаблон("|%1", ТекущаяСтрока);
				
				Если Сч = КоличествоСтрок Тогда
					ТекущаяСтрока = СтрШаблон("%1%2", ТекущаяСтрока, """");
				КонецЕсли;
			КонецЕсли;
			
			ТекстовыйДокумент.ЗаменитьСтроку(Сч, ТекущаяСтрока);
		КонецЦикла;
	Иначе
		
		Для Сч = 1 По КоличествоСтрок Цикл
		
			ТекущаяСтрока = ТекстовыйДокумент.ПолучитьСтроку(Сч);
			
			Если Сч = 1 Тогда
				ТекущаяСтрока = Сред(ТекущаяСтрока, 2);
			ИначеЕсли Сч = КоличествоСтрок Тогда
				ТекущаяСтрока = Сред(ТекущаяСтрока, 1, СтрДлина(ТекущаяСтрока) - 1);
			КонецЕсли;
			
			ТекущаяСтрока = СтрЗаменить(ТекущаяСтрока, """""", """");
			ТекущаяСтрока = СтрЗаменить(ТекущаяСтрока, "|", "");
			
			ТекстовыйДокумент.ЗаменитьСтроку(Сч, ТекущаяСтрока);
		КонецЦикла;
	КонецЕсли;
	
	Результат = ТекстовыйДокумент.ПолучитьТекст();
	Результат = Сред(Результат, 1, СтрДлина(Результат) - 1);
	
	Возврат Результат;
	
КонецФункции

// Превращает строку camelCase в синоним
//
// Параметры:
//  ИсходнаяСтрока - Строка - Строка типа ИдентификаторНастройки
// 
// Возвращаемое значение:
//  Строка - Синоним
//
Функция СинонимСтроки(ИсходнаяСтрока) Экспорт
	
	Результат = пбп_ПереадресацияКлиентСервер.РазложитьСтрокуВМассивПодстрок(ИсходнаяСтрока);
	Если Результат.Количество() > 1 Тогда
		Возврат "";
	КонецЕсли;
	
	ДлинаСтроки = СтрДлина(ИсходнаяСтрока) + 1;
	Старт = 1;
	Конец = 0;
	Части = Новый Массив;
	Для Конец = 2 По ДлинаСтроки Цикл
		КодСимвола = КодСимвола(Сред(ИсходнаяСтрока, Конец, 1));
		Если СимволВерхнегоРегистра(КодСимвола) Или Конец = ДлинаСтроки Тогда
			КолСимволов = Конец - Старт;
			Части.Добавить(Сред(ИсходнаяСтрока, Старт, ?(КолСимволов = 0, ДлинаСтроки - Старт, КолСимволов)));
			Старт = Конец;
		КонецЕсли;
	КонецЦикла;
	Синоним = НРег(СтрСоединить(Части, " "));
	Синоним = ВРег(Лев(Синоним, 1)) + Сред(Синоним,2);
	
	Возврат Синоним;
	
КонецФункции

// Преобразовать дату из строки по строковому формату
//
// Параметры:
//  ВхЗначение	 - Строка - дата строкой
//  НомерФормата - Строка - номер формата с разделителями "_". Подробнее см.
//  в пбп_ЗагрузкаФайлаЧерезТабличныйДокументСервер.ПолучитьНомерФорматаДатыИзСтроки
// 
// Возвращаемое значение:
//  Дата - дата, полученная из преобразования даты строкой по формату
//
Функция ПреобразоватьДатуИзСтрокиПоФормату(ВхЗначение, НомерФормата) Экспорт

	ПреобразованнаяДата = Дата(1, 1, 1, 0, 0, 0);
	
	Если ПустаяСтрока(ВхЗначение) ИЛИ ПустаяСтрока(НомерФормата) Тогда
		Возврат ПреобразованнаяДата;
	КонецЕсли;
	
	Попытка
	Если НомерФормата = "0_0_0" Тогда
		#Если Не ВебКлиент Тогда
			Попытка
				СтрокаДаты = Строка(ВхЗначение);
				ПозицияТочки = СтрНайти(СтрокаДаты, ".");
				Если ПозицияТочки > 0 Тогда
					// Отсекаем миллисекунды, т.к. XMLЗначение их не поддерживает
					СтрокаДаты = Лев(СтрокаДаты, ПозицияТочки - 1);
				КонецЕсли;
				ПреобразованнаяДата = XMLЗначение(Тип("Дата"), СтрокаДаты);
			Исключение
				ПреобразованнаяДата = Дата(1, 1, 1, 0, 0, 0);
			КонецПопытки;
		#Иначе
			// В веб-клиенте XMLЗначение и СтрокаВЧисло недоступны, используем альтернативный способ парсинга ISO даты
			Попытка
				// Пытаемся парсить ISO строку вида "2024-01-02T03:04:05"
				ДатаВремяСтрока = Строка(ВхЗначение);
				Если СтрНайти(ДатаВремяСтрока, "T") > 0 Тогда
					ЧастиДатаВремя = СтрРазделить(ДатаВремяСтрока, "T");
					ЧастиДаты = СтрРазделить(ЧастиДатаВремя[0], "-");
					ЧастиВремени = СтрРазделить(ЧастиДатаВремя[1], ":");
					
					// Используем безопасное преобразование для веб-клиента
					Год = БезопасноПреобразоватьСтрокуВЧисло(ЧастиДаты[0], 1);
					Месяц = БезопасноПреобразоватьСтрокуВЧисло(ЧастиДаты[1], 1);
					День = БезопасноПреобразоватьСтрокуВЧисло(ЧастиДаты[2], 1);
					
					Часы = БезопасноПреобразоватьСтрокуВЧисло(ЧастиВремени[0], 0);
					Минуты = БезопасноПреобразоватьСтрокуВЧисло(ЧастиВремени[1], 0);
					
					// Обрабатываем секунды (могут быть с миллисекундами)
					СекундыСтрока = ЧастиВремени[2];
					Если СтрНайти(СекундыСтрока, ".") > 0 Тогда
						ЧастиСекунды = СтрРазделить(СекундыСтрока, ".");
						Секунды = БезопасноПреобразоватьСтрокуВЧисло(ЧастиСекунды[0], 0);
					Иначе
						Секунды = БезопасноПреобразоватьСтрокуВЧисло(СекундыСтрока, 0);
					КонецЕсли;
					
					ПреобразованнаяДата = Дата(Год, Месяц, День, Часы, Минуты, Секунды);
				Иначе
					ПреобразованнаяДата = Дата(1, 1, 1, 0, 0, 0);
				КонецЕсли;
			Исключение
				ПреобразованнаяДата = Дата(1, 1, 1, 0, 0, 0);
			КонецПопытки;
		#КонецЕсли
	Иначе
		РазрядыФормата = пбп_ПереадресацияКлиентСервер.РазложитьСтрокуВМассивПодстрок(НомерФормата, "_");
		
			// Инициализация переменных
			Год = 1; Месяц = 1; День = 1;
			Часы = 0; Минуты = 0; Секунды = 0;
			
			// Разделяем дату и время
			МассивДатаВремя = СтрРазделить(ВхЗначение, " ", 2);
			ТолькоДата = МассивДатаВремя[0];
			ТолькоВремя = ?(МассивДатаВремя.Количество() > 1, МассивДатаВремя[1], "");
			
			// Парсинг даты
			РазделительДаты = "";
			Если РазрядыФормата[0] = "1" Тогда РазделительДаты = ".";
			ИначеЕсли РазрядыФормата[0] = "2" Тогда РазделительДаты = "/";
			ИначеЕсли РазрядыФормата[0] = "3" Тогда РазделительДаты = "-";
				КонецЕсли;
				
			Если Не ПустаяСтрока(РазделительДаты) Тогда
				МассивГодМесяцДень = СтрРазделить(ТолькоДата, РазделительДаты);
				Если МассивГодМесяцДень.Количество() = 3 Тогда
					Если РазрядыФормата[1] = "0" Тогда // MMDDYYYY
						Год = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[2], 1);
						Месяц = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[0], 1);
						День = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[1], 1);
					ИначеЕсли РазрядыФормата[1] = "1" Тогда // YYYYMMDD
						Год = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[0], 1);
						Месяц = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[1], 1);
						День = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[2], 1);
					ИначеЕсли РазрядыФормата[1] = "2" Тогда // DDMMYYYY
						Год = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[2], 1);
						Месяц = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[1], 1);
						День = БезопасноПреобразоватьСтрокуВЧисло(МассивГодМесяцДень[0], 1);
			КонецЕсли;
		КонецЕсли;
			Иначе // формат без разделителей
				Если СтрДлина(ТолькоДата) = 8 Тогда
					Если РазрядыФормата[1] = "0" Тогда // MMDDYYYY
						Год = БезопасноПреобразоватьСтрокуВЧисло(Сред(ТолькоДата, 5, 4), 1);
						Месяц = БезопасноПреобразоватьСтрокуВЧисло(Лев(ТолькоДата, 2), 1);
						День = БезопасноПреобразоватьСтрокуВЧисло(Сред(ТолькоДата, 3, 2), 1);
					ИначеЕсли РазрядыФормата[1] = "1" Тогда // YYYYMMDD
						Год = БезопасноПреобразоватьСтрокуВЧисло(Лев(ТолькоДата, 4), 1);
						Месяц = БезопасноПреобразоватьСтрокуВЧисло(Сред(ТолькоДата, 5, 2), 1);
						День = БезопасноПреобразоватьСтрокуВЧисло(Прав(ТолькоДата, 2), 1);
					ИначеЕсли РазрядыФормата[1] = "2" Тогда // DDMMYYYY
						Год = БезопасноПреобразоватьСтрокуВЧисло(Прав(ТолькоДата, 4), 1);
						Месяц = БезопасноПреобразоватьСтрокуВЧисло(Сред(ТолькоДата, 3, 2), 1);
						День = БезопасноПреобразоватьСтрокуВЧисло(Лев(ТолькоДата, 2), 1);
			КонецЕсли;
			КонецЕсли;
		КонецЕсли;
		
			// Парсинг времени
			Если РазрядыФормата[2] <> "0" И Не ПустаяСтрока(ТолькоВремя) Тогда
				// Удаляем AM/PM индикаторы для простоты
				ВремяБезAmPm = СтрЗаменить(СтрЗаменить(ТолькоВремя, " AM", ""), " PM", "");
				МассивВремя = СтрРазделить(ВремяБезAmPm, ":");
				
				Если МассивВремя.Количество() > 0 Тогда Часы = БезопасноПреобразоватьСтрокуВЧисло(МассивВремя[0]); КонецЕсли;
				Если МассивВремя.Количество() > 1 Тогда Минуты = БезопасноПреобразоватьСтрокуВЧисло(МассивВремя[1]); КонецЕсли;
				Если МассивВремя.Количество() > 2 Тогда Секунды = БезопасноПреобразоватьСтрокуВЧисло(МассивВремя[2]); КонецЕсли;
				
				// Корректировка для формата 12-часов (AM/PM)
				Если РазрядыФормата[2] = "2" Тогда
					Если СтрНайти(ВРег(ТолькоВремя), "PM") > 0 И Часы <> 12 Тогда
						Часы = Часы + 12;
					ИначеЕсли СтрНайти(ВРег(ТолькоВремя), "AM") > 0 И Часы = 12 Тогда
						Часы = 0; // Полночь
					КонецЕсли;
				КонецЕсли;
		КонецЕсли;
		
			ПреобразованнаяДата = Дата(Год, Месяц, День, Часы, Минуты, Секунды);
		
	КонецЕсли;
	Исключение
		ПреобразованнаяДата = Дата(1, 1, 1, 0, 0, 0);
	КонецПопытки;
	
	Возврат ПреобразованнаяДата;
	
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Безопасное преобразование строки в число для веб-клиента
Функция БезопасноПреобразоватьСтрокуВЧисло(СтрокаЗначение, ЗначениеПоУмолчанию = 0)
	
	Попытка
		Возврат Число(СтрокаЗначение);
	Исключение
		Возврат ЗначениеПоУмолчанию;
	КонецПопытки;
	
КонецФункции

Функция СимволВерхнегоРегистра(КодСимвола)
	
	минКирилица = 1040;
	максКирилица = 1070;
	минЛатиница = 65;
	максЛатиница = 90;
	
	Если (КодСимвола >= минКирилица И КодСимвола <= максКирилица)
		Или (КодСимвола >= минЛатиница И КодСимвола <= максЛатиница) Тогда
		Возврат Истина;
	КонецЕсли;
		
	Возврат Ложь;
	
КонецФункции

Функция ПолучитьТолькоДатуЕслиРазделительПробел(МассивРазделителейДатаВремя) Экспорт
	
	ТолькоДата = "";
	
	КоличествоСмещений = 2;
	КоличествоРазделителей = МассивРазделителейДатаВремя.Количество() - КоличествоСмещений; // В последнем часы
	Для Разделитель = 0 По КоличествоРазделителей Цикл
		ТолькоДата = ТолькоДата + ?(ПустаяСтрока(ТолькоДата), "", " ") + МассивРазделителейДатаВремя[Разделитель];
	КонецЦикла;
	
	Возврат ТолькоДата;
	
КонецФункции

#КонецОбласти
